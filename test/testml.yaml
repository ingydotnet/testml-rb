source: ../../testml-lite-tml  # merge with testml-tml
target: ./testml
template: |
  require 'testml'
  require 'testml_bridge'

  TestML.new(
    tmlfile: '%{tmlfile}',
    bridge: TestMLBridge,
  )
